import React, { Component } from 'react';

export default class SearchBar extends Component {
  render () {
    return (
      <from>
               <input placeholder='Search DailySmarty'></input>
      </from>
    );
  }
}